const addresses = require("./addresses");
const link = require("./link");
const neigh = require("./neighbors");
const utils = require("./utils");

module.exports = {
    addr: addresses,
    link: link,
    neigh: neigh,
    utils: utils,
};
